[pytest]
python_files = *
python_functions = [!_]*

addopts =
  --benchmark-enable
  --benchmark-verbose
  --benchmark-autosave
  --benchmark-disable-gc
  --benchmark-warmup=on
  --benchmark-min-rounds=10
  --benchmark-warmup-iterations=30
  -p no:xdist