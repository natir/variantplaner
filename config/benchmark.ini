[pytest]
python_files = *
python_functions = [!_]*

addopts =
  --benchmark-enable
  --benchmark-verbose
  --benchmark-autosave
  --benchmark-disable-gc
  --benchmark-warmup=on
  --benchmark-warmup-iterations=100
  -p no:xdist