[pytest]
python_files = *
python_functions = *

addopts =
  --benchmark-enable
  --benchmark-verbose
  --benchmark-autosave
  --benchmark-disable-gc
  --benchmark-histogram=htmlcov/bench/graph
  -p no:xdist