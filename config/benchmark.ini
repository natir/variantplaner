[pytest]
python_files = *
python_functions = [!_]*

addopts =
  --benchmark-enable
  --benchmark-verbose
  --benchmark-autosave
  --benchmark-disable-gc
  --benchmark-histogram=htmlcov/bench/graph
  --benchmark-warmup=on
  -p no:xdist